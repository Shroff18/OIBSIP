function call() {
    const ipt = document.getElementById('inputtxt').value
    const psw = document.getElementById('password').value
    if (ipt == "" || psw == "") {
        // alert("fill the form ")
    }
    else {
        alert("succesfully submit")
    }
}
